# 00 — 环境搭建指南

> **目标**: 从零开始，在你的电脑上把 ScratchV 编译器跑起来。
> **预计时间**: 15-30 分钟
> **前置要求**: 会用终端（命令行），电脑能上网

---

## 目录

1. [你需要什么](#1-你需要什么)
2. [克隆代码](#2-克隆代码)
3. [创建虚拟环境](#3-创建虚拟环境)
4. [安装 ScratchV](#4-安装-scratchv)
5. [验证安装](#5-验证安装)
6. [安装可选依赖](#6-安装可选依赖)
7. [平台特定说明](#7-平台特定说明)
8. [检查清单](#8-检查清单)

---

## 1. 你需要什么

| 软件 | 版本要求 | 怎么检查 |
|------|---------|---------|
| Python | 3.8 或更高（推荐 3.12） | `python3 --version` |
| Git | 任意版本 | `git --version` |
| pip | 通常随 Python 安装 | `pip --version` |

### 如果你还没有安装

**Ubuntu / Debian / WSL**:
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv git
```

**macOS**:
```bash
# 先安装 Homebrew（如果还没有）: https://brew.sh
brew install python@3.12 git
```

**Windows**:
- 推荐安装 [WSL2](https://learn.microsoft.com/zh-cn/windows/wsl/install)（Windows Subsystem for Linux），然后在 WSL 里按 Ubuntu 的方式操作
- 或者从 [python.org](https://www.python.org/downloads/) 下载 Python 安装包

> 💡 **为什么推荐 WSL？** ScratchV 的 RISC-V 仿真工具（TinyFive、Spike）主要在 Linux 上测试过。WSL 让你在 Windows 上拥有一个完整的 Linux 环境，体验最好。

---

## 2. 克隆代码

打开终端，进入你放项目的目录：

```bash
# 进入你喜欢的目录（比如 ~/workspace）
cd ~/workspace   # 如果没有这个目录，先 mkdir ~/workspace

# 克隆 ScratchV 仓库
git clone https://github.com/kinsomwang/ScratchV.git
cd ScratchV
```

预期输出（类似）：
```
Cloning into 'ScratchV'...
remote: Enumerating objects: 1234, done.
remote: Counting objects: 100% (1234/1234), done.
...
```

> 💡 **网络慢怎么办？** 如果 GitHub 访问慢，可以在项目目录下配置代理：
> ```bash
> git config http.proxy http://127.0.0.1:7890   # 替换为你的代理地址
> ```

---

## 3. 创建虚拟环境

虚拟环境让你的 ScratchV 依赖和系统 Python 隔离开，避免版本冲突。

```bash
# 在 ScratchV 目录下创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate    # Linux / macOS / WSL
# 或者
venv\Scripts\activate       # Windows CMD
```

激活成功后，你的终端提示符前面会出现 `(venv)`：
```
(venv) user@computer:~/workspace/ScratchV$
```

> 💡 **怎么退出虚拟环境？** 输入 `deactivate` 即可。

---

## 4. 安装 ScratchV

```bash
# 确保在 ScratchV 目录下，且虚拟环境已激活
pip install -e .
```

预期输出（类似）：
```
Obtaining file:///home/user/workspace/ScratchV
Installing build dependencies ... done
...
Successfully installed scratchv-0.3.0
```

`-e` 参数是 "editable" 模式，意味着你修改源码后不需要重新安装，直接生效。

---

## 5. 验证安装

运行测试，确认一切正常：

```bash
# 快速测试（约 1-2 分钟）
make test
```

预期输出（类似）：
```
tests/test_frontend.py::test_dsl_basic PASSED
tests/test_ir_builder.py::test_build_add PASSED
...
==================== 50+ passed in 12.34s ====================
```

如果测试全部通过（绿色 `PASSED`），恭喜！环境搭建完成 🎉

### 快速试跑：编译一个 ONNX 模型

```bash
# 编译 CNN 模型，生成 RISC-V 机器码
python3 scratchv/standalone/onnx_to_riscv_standalone.py models/graph/cnn.onnx \
    -o /tmp/cnn_test.bin --estimate --report
```

预期输出：
```
=== ScratchV ONNX → RISC-V Compiler ===
Model: models/graph/cnn.onnx
Nodes: 9 (3 Conv + 3 MaxPool + 2 Gemm + 1 Relu)
Static instructions: 749
Estimated dynamic instructions: 3,222,000,000
Report: benchmark_reports/cnn_riscv_report.md
Done.
```

---

## 6. 安装可选依赖

ScratchV 的核心编译器**零外部依赖**（只用 Python 标准库）。以下依赖是可选的，按需安装：

```bash
# 基础安装（核心编译器，零外部依赖）
pip install -e .

# TinyFive — RISC-V 仿真器，用来实际"跑"编译出的代码
pip install tinyfive

# llvmlite — LLVM IR 生成和 JIT 执行
pip install llvmlite

# onnxruntime — 用来验证 ScratchV 编译结果的正确性
pip install onnxruntime

# 一键安装全部可选依赖
pip install -e ".[all]"
```

各依赖的用途：

| 依赖 | 用途 | 什么时候需要 |
|------|------|------------|
| `tinyfive` | RISC-V 仿真执行 | 想真正运行编译出的代码、看执行结果 |
| `llvmlite` | 生成 LLVM IR | 想对比 ScratchV 和 LLVM 的编译效果 |
| `onnxruntime` | 标准 ONNX 推理 | 想验证 ScratchV 计算结果是否正确 |

---

## 7. 平台特定说明

### Ubuntu 22.04 / 24.04

```bash
# 确保系统包齐全
sudo apt update
sudo apt install python3 python3-pip python3-venv python3-dev git build-essential
```

### macOS (Apple Silicon / Intel)

```bash
# Xcode Command Line Tools（提供 git、make 等）
xcode-select --install

# 后续步骤同上
```

### WSL2 (Windows)

```bash
# WSL 里的 Ubuntu 和原生 Ubuntu 几乎一样
sudo apt update
sudo apt install python3 python3-pip python3-venv python3-dev git build-essential
```

### 国内用户加速 pip

```bash
# 临时使用清华镜像
pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者永久配置
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 8. 检查清单

完成以下所有项，你的环境就准备好了：

- [ ] Python 版本 ≥ 3.8（`python3 --version` 确认）
- [ ] 代码已克隆到本地（`git status` 正常）
- [ ] 虚拟环境已创建并激活（终端提示有 `(venv)`）
- [ ] `pip install -e .` 成功
- [ ] `make test` 全部通过
- [ ] 能成功编译 CNN 模型（`make bench-cnn`）

---

## 遇到问题？

跳到 [04-故障排除FAQ.md](04-故障排除FAQ.md) 查找常见问题的解决方案。

如果还是解决不了，欢迎到 [GitHub Issues](https://github.com/kinsomwang/ScratchV/issues) 提问。

---

> **下一步**: 环境搭好了，去看看 [01-编译器概念入门.md](01-编译器概念入门.md)，理解编译器到底在做什么。
